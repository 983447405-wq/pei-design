# Styles

Global application styles currently live in `app/globals.css` because the project uses the Next.js App Router.

Use this folder for future shared style documentation, token exports, or non-global style assets.
